# Build a CubeSat – bac-page environment
# Copy to .env and fill in values. Never commit .env.
#
# The repo path normally lives in ~/.config/bac/bac-page.toml (run `bac-page --init`).
# BAC_PAGE_REPO overrides the config file when set.

BAC_PAGE_REPO=
